apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-trivial
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Release.Name }}-trivial
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}-trivial
    spec:
      containers:
        - name: trivial
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.service.port }}
