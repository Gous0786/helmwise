apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "with-helpers.fullname" . }}
  labels:
    {{- include "with-helpers.labels" . | nindent 4 }}
data:
  log-level: {{ .Values.config.logLevel | quote }}
